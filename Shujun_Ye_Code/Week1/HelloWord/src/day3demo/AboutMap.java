package day3demo;

import java.util.HashMap;
import java.util.LinkedHashMap;
import java.util.Map;
import java.util.TreeMap;

public class AboutMap {

	public static void main(String[] args) {
		Map<Integer, String> workHashMap = new HashMap<>();
		Map<String, String> workLinkedHashMap = new LinkedHashMap<>();
		Map<String, String> workTreeMap = new TreeMap<>();
		
		// put(k, v) method associates the specified value v with the specified key k.
		// If the map already contains a mapping for the key, the old value is replaced
		// by the specified value.
		workTreeMap.put(".c", "C");
		
		
		// get(Object key) method returns the value associated with the specified key, or
		// return null if the map contains no mapping for the key.
		
		// containsKey(Object key) / containsValue(Object value) returns true if the map
		// contains a mapping for the specified key
		
		
		
		

	}

}
